import 'package:flutter/material.dart';

class GoogleService extends StatelessWidget {
  const GoogleService({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
